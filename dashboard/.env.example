# Base URL of the Heimdal backend API
NEXT_PUBLIC_API_URL=http://localhost:8080

# Set to "true" to run the dashboard against in-memory mock data (no backend required).
# Any email + password logs you in, and all pages render sample data.
NEXT_PUBLIC_USE_MOCKS=false
